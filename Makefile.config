CC = gcc
CFLAGS = -g
OUTPUT_OPTION = -MMD -MP -o $@

# Linux
REMOVE = rm -rf
SHARED_LIBRARY_EXTENSION = .so
SHARED_LIBRARY_FLAG = -shared

# Windows
#REMOVE = rm -rf
#SHARED_LIBRARY_EXTENSION = .so
#SHARED_LIBRARY_FLAG = -shared

